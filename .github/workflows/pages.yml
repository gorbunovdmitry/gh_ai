# Этот файл отключает автоматическое развертывание GitHub Pages
# Мы используем ручное развертывание через ветку gh-pages
name: Disable Pages Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # Пустой job, который ничего не делает
  disable-pages:
    runs-on: ubuntu-latest
    steps:
      - name: Disable automatic Pages deployment
        run: echo "Pages deployment is handled manually via gh-pages branch" 